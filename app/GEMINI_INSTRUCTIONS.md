# Gemini Instructions - App Module

This file provides instructions for Gemini agents working on the `app` module.

- The main class is `com.fizzpod.starter.Main`.
- Tests are located in `src/test/java`.
- Ensure `app/build.gradle` is updated if new dependencies are added.
